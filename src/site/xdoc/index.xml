<?xml version="1.0" encoding="UTF-8"?>
<!--
    Copyright 2024 Bloomreach (https://www.bloomreach.com)

    Licensed under the Apache License, Version 2.0 (the  "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS"
    BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
-->
<document>
  <properties>
    <title>Bloomreach XM Extra Folder Context Menus</title>
  </properties>
  <body>
    <section name="Bloomreach XM Extra Folder Context Menus">
      <p>
        This project provides extra Folder Context Menu items such as 'Copy folder...' and 'Move folder...'
        for Bloomreach XM with extensible base implementations for developers.
      </p>
      <p>
        The best way to explain this feature is probably to show screenshots. After installing this plugin, you will be able to see 'Copy folder...' and 'Move folder...' context menus.
      </p>

      <img alt="Folder copy and move context menu"
           src="images/foldercpmvmenus.png"/>

      <p><strong>Copying folders</strong></p>

      <p>
        In the Folder Copy dialog, you can select the destination folder, folder name and folder URL name.
      </p>
      <img alt="Folder copy dialog"
           src="images/copyfolder1.png" />

      <p>
        Please try to click on the "See Also" links field to open the linked documents.
        The document links are automatically reset to the corresponding copied documents
        under the same copied root folder if the linked document was under the same root folder.
      </p>
      <img alt="After copying a folder"
           src="images/copyfolder2.png" />

      <p><strong>Copying and translating folders</strong></p>

      <p>When copying translated folders, there is the option of linking the copied folders and documents as translations.
         This makes sense when copying between root folders that have different locales but are translations of each other.
         The default value for this option can be configured with <code>default.linkTranslation: true</code> (<code>false</code> by default)
         on the folder configurations in <code>/hippo:configuration/hippo:workflows/threepane/</code>.
      </p>

      <img alt="Folder copy with translation dialog"
           src="images/copyfolder3.png" />
      <p>
        As you can see from the flags, the documents have been linked.
      </p>

      <img alt="After copying with translation a folder"
           src="images/copyfolder4.png" />

      <p><strong>Moving folders</strong></p>
      <p>
        In the Folder Move dialog, you can select the destination folder, folder name and folder URL name.
      </p>
      <img alt="Folder move dialog"
           src="images/movefolder1.png" />

      <p>
        Please try to click on the "See Also" links field to open the linked documents.
        In this case, the document links are not affected by moving the folder
        because all the internal document lnks are based on node identifiers (UUIDs).
      </p>
      <img alt="After moving a folder"
           src="images/movefolder2.png" />

      <p><strong>Progress indicator</strong></p>
      <p>
        When copying or moving folders with many items, a progress indicator is displayed showing:
      </p>
      <ul>
        <li>The current item count and total (e.g., "Processing 45/293 items")</li>
        <li>A visual progress bar with percentage complete</li>
        <li>The path of the item currently being processed</li>
        <li>Estimated time remaining</li>
        <li>A cancel button to abort long-running operations</li>
      </ul>
      <p>
        This provides visibility into the operation's progress and allows users to cancel if needed.
      </p>

      <p><strong>Operation behavior differences</strong></p>
      <p>
        Copy and move operations behave differently due to their underlying implementation:
      </p>
      <ul>
        <li><strong>Move</strong> uses JCR's native <code>Session.move()</code> which is atomic - the entire folder tree
            is relocated instantly regardless of size. The progress bar tracks post-move processing (path recomputation),
            not the move itself.</li>
        <li><strong>Copy</strong> creates nodes one by one, so progress accurately reflects the copying process.
            Larger folders take proportionally longer.</li>
      </ul>
      <p>
        For small folders or move operations, the dialog may show the completion summary immediately
        because the operation finishes before the progress display fully renders.
      </p>

      <p><strong>Cancellation behavior</strong></p>
      <p>
        When an operation is cancelled:
      </p>
      <ul>
        <li><strong>Copy cancelled</strong>: Already-copied content remains at the destination. The plugin automatically
            runs post-processing on partial content: documents are marked as unpublished, internal links are updated,
            and translation IDs are reset. You may want to review the destination and either delete the partial copy
            or complete it manually.</li>
        <li><strong>Move cancelled</strong>: Since move is atomic, the folder has already been fully relocated.
            Cancellation only affects post-move path recomputation. Content is safe and accessible.</li>
      </ul>
      <p>
        The completion summary shows how many items were processed before cancellation
        (e.g., "Cancelled after copying 45 items").
      </p>

      <p><strong>Post-processing steps</strong></p>
      <p>
        After copying, the plugin performs important cleanup:
      </p>
      <ul>
        <li><strong>Take documents offline</strong>: Copied documents are marked as unpublished to prevent
            duplicate published content on your site.</li>
        <li><strong>Reset internal links</strong>: <code>hippo:docbase</code> references within copied content
            are updated to point to the corresponding copied nodes.</li>
        <li><strong>Reset translation IDs</strong>: New UUIDs are generated for <code>hippotranslation:id</code>
            properties to prevent translation conflicts (unless linking as translation is enabled).</li>
      </ul>
      <p>
        These steps run automatically, even after cancellation, on any content that was successfully copied.
      </p>

      <p><strong>Known limitations</strong></p>
      <p>
        <em>Move operation dialog may close before summary is viewed:</em> In rare cases, the progress dialog
        may close automatically after a move operation completes, before the user has a chance to review the
        summary. This occurs because:
      </p>
      <ul>
        <li>Move operations delete the source folder from JCR</li>
        <li>When <code>session.save()</code> persists changes, JCR fires repository events</li>
        <li>The CMS UI may respond to these events by refreshing, which closes modal dialogs</li>
      </ul>
      <p>
        This is a CMS framework behavior, not a bug in this plugin. The operation completes successfully;
        only the summary display is affected. Copy operations are not affected because they do not remove
        the source folder. All operations are logged to the server logs with operation ID, user, source path,
        destination path, and item count for traceability.
      </p>

    </section>

    <section name="Programmatic Folder Copy and Move">
      <p>
        It is also possible to use the library in programmatic way (e.g, in Java code or Groovy script) to copy or move a folder.
        In that case, refer to the
        <a href="install.html#Installing_Only_the_Common_module_of_Hippo_CMS_Extra_Folder_Context_Menus_Plugin">Installing Only the Common module of Bloomreach XM Extra Folder Context Menus Plugin</a> section.
      </p>

      <p>For folder copy, see the following Java code example:</p>
      <div class="brush: java">
        <source><![CDATA[
import org.onehippo.forge.folderctxmenus.common.FolderCopyTask;

        Session jcrSession = ...; // you get session somehow, depending on context.

        try {
            String sourceFolderPath = "/content/documents/myhippoproject/news";
            String targetFolderPath = "/content/documents/myhippoproject";
            String destFolderNodeName = "news2";
            String destFolderDisplayName = "News 2";
            Boolean resetTranslations = true;

            Node sourceFolderNode = jcrSession.getNode(sourceFolderPath);
            Node targetFolderNode = jcrSession.getNode(targetFolderPath);

            FolderCopyTask task =
                    new FolderCopyTask(jcrSession, Locale.ENGLISH,
                                       sourceFolderNode, targetFolderNode,
                                       destFolderNodeName, destFolderDisplayName,
                                       resetTranslations);

            // copy /content/documents/myhippoproject/news to
            //      /content/documents/myhippoproject/news2
            task.execute();

            jcrSession.save();
        } finally {
            jcrSession.refresh(false);
        }
        ]]></source>
      </div>

      <p>For folder move, see the following Java code example:</p>
      <div class="brush: java">
        <source><![CDATA[
import org.onehippo.forge.folderctxmenus.common.FolderMoveTask;

        Session jcrSession = ...; // you get session somehow, depending on context.

        try {
            String sourceFolderPath = "/content/documents/myhippoproject/news/2011";
            String targetFolderPath = "/content/documents/myhippoproject/common";
            String destFolderNodeName = "2011";
            String destFolderDisplayName = "2011";

            Node sourceFolderNode = jcrSession.getNode(sourceFolderPath);
            Node targetFolderNode = jcrSession.getNode(targetFolderPath);

            FolderMoveTask task =
                    new FolderMoveTask(jcrSession, Locale.ENGLISH,
                                       sourceFolderNode, targetFolderNode,
                                       destFolderNodeName, destFolderDisplayName);

            // move /content/documents/myhippoproject/news/2011 to
            //      /content/documents/myhippoproject/common/2011
            task.execute();

            jcrSession.save();
        } finally {
            jcrSession.refresh(false);
        }
        ]]></source>
      </div>
    </section>

    <section name="Demo Application">

      <p>
        Fork me on GitHub: <a href="https://github.com/bloomreach-forge/folder-context-menus" target="_blank">https://github.com/bloomreach-forge/folder-context-menus</a>.
      </p>
      <p>
        Build the project using Maven:
      </p>
      <div class="brush: bash">
      <source>$ mvn install</source>
      </div>
      <p>And, build and run the demo in the <code>demo</code> subfolder.</p>
      <div class="brush: bash">
      <source><![CDATA[
$ cd demo
$ mvn clean package
$ mvn -P cargo.run
        ]]></source>
      </div>
      <p>
        Visit http://localhost:8080/cms/.
      </p>

    </section>

  </body>
</document>
